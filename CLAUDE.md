# claude-task-orchestrator

常駐オーケストレータの中核。

## 取り込む側の事情を持ち込まない

**core は取り込む側の事情を知らない。** README の「取り込んで使う」に書いてある設計上の分担だが、
同時に**書いてよいものの境界**でもある。取り込む側は非公開であることが多く、そこにしか意味の無い
固有名がここへ混ざると、取り込んだ人は自分の内情を公開したことになる。

書かないのは、組織名・製品名・システム名とその略称、課題管理の識別子（`ABC-1234` の形をしたもの）
とその URL、リポジトリ名、ホスト名、人名。**コード・コメント・テスト・ドキュメント・
コミットメッセージ・PR のタイトルと本文・レビュー返信・issue**、全てが対象である。

**例示にも使わない。** 危ないのは機能そのものより例のほうで、JSDoc の使用例、エラーメッセージの
中の例、テストの入力値は、差分の上では「ただの例」に見えるまま通る。例が要るときは、その場で
考えた汎用名（`webapp-backend`、`example-service`、`TASK-1` のような）を使う。

**迷ったら書かない。** 消すのは後から効かない。公開リポジトリでは、force push した後も旧コミットは
SHA を知っていれば参照できる状態でしばらく残る。

## 何が守っているか

**このリポジトリには門が無い。** 照合する語を持ち込めないためである。伏せたい語の一覧は、それ自体が
伏せたい情報である。ハッシュにしても変わらない——候補語を一つずつ当てて確かめられる程度の大きさしか
ないので、公開すれば「その語が載っているか」に答える装置になる。

門は取り込む側に置く。あちらは自分の語を平文で持てるし、core を直すのはたいていあちらの文脈の中なので、
そこで走らせれば押し出す前に止まる。`docs/embedding.md` に書き方がある。

したがってここでは、**上の規則が最初で最後の判断になる。**

## 依存の向き

core は取り込む側を import しない。プロンプトの中では `{{coreDir}}` がこのリポジトリ、
`{{repoDir}}` が取り込む側を指す。詳しくは `docs/embedding.md`。
